<Window x:Class="MenuBarApp.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MenuBarApp.Views"
             mc:Ignorable="d"        
             WindowState="Maximized"
             WindowStyle="None">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Barra superior de navegación -->
        <StackPanel Orientation="Horizontal" Background="DarkSlateGray">
            <Button Content="Cliente" Width="200" Height="160" Margin="5" Click="BtnCliente_Click"/>
            <Button Content="Cocina" Width="200" Height="160" Margin="5" Click="BtnCocina_Click"/>
            <Button Content="Barra" Width="200" Height="160" Margin="5" Click="BtnBarra_Click"/>

        </StackPanel>

    </Grid>

</Window>
